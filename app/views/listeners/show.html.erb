<% provide(:title, @listener.name) %>
<div class="row">
	<aside class="span4">
		<section>
			<h1>
				<%= image_tag(@listener.image, alt: @listener.name) %>
				<%= @listener.name %>
			</h1>
				
		</section>
	</aside>
</div>
<br>
<div>
	<%= @listener.type %>
	<%= @listener.location %>
	<%= @listener.oauth_token %>
	<%= link_to "Share", new_share_path %>


</div>
  