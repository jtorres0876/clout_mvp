<header class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
        	<%= link_to image_tag("tclogo2.png", size: "100x30",  alt: "Tuneclout"), '#', id: "logo"  %>
          <nav>

          	<form class="navbar-search">
  					<input type="text" class="search-query" placeholder="Search">
  			    </form> 
            <ul class="nav pull-right">
              <li><%= link_to "Home",    root_path %></li>
              <li class="divider-vertical"></li>
              <li><%= link_to "Help",    '#' %></li>
              <li class="divider-vertical"></li>
              <% if signed_in? %>
                  <li id="fat-menu" class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                      Account <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                      <li><%= link_to "Profile", current_listener %></li>
                      <% if is_artist?%>
                        <li><%= link_to "Edit Profile", edit_artist_path(current_listener) %></li>
                        <% if current_listener.admin? %>
                          <li><%= link_to "All Artists", artists_path %></li>
                        <% end %>  
                      <% end %>
                      <li class="divider"></li>
                      <li><%= link_to "Sign out", signout_path %>
                      </li>
                    </ul>
                  </li>
              <% end %>
            </ul>  
          </nav>
        </div>
      </div>
</header>